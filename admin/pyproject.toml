# Owner: Charbel

[project]
name = "concierge-admin"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.36",
    "hvac>=2.3",
    "requests>=2.31",
]

[tool.ruff]
line-length = 100
target-version = "py312"
